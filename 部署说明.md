# 广告宣传材料合规检查 Agent：GitHub Pages 部署说明

## 这个包已经针对目标仓库配置

- 目标仓库：`yunyuzhu2005/ad-compliance-checker`
- 预期 Pages 地址：`https://yunyuzhu2005.github.io/ad-compliance-checker/`
- 静态资源路径已使用 `/ad-compliance-checker/`
- 独立版本不依赖快手企业 SSO 或内部站点权限接口

## 包内文件

- `index.html`：网站入口，必须放在仓库根目录
- `static/`：网站样式和脚本，必须完整上传
- `.nojekyll`：关闭 Jekyll 处理

## 网页端上传步骤

1. 解压 `广告宣传材料合规检查Agent-GitHub-Pages.zip`。
2. 打开仓库 `https://github.com/yunyuzhu2005/ad-compliance-checker`。
3. 建议先将仓库原来的 `index.html` 下载备份或改名为 `legacy-index.html`。
4. 点击 **Add file → Upload files**。
5. 上传解压后的 `index.html`、`static` 文件夹和 `.nojekyll`。
6. Commit message 填写 `Deploy advertising compliance agent`，点击 **Commit changes**。
7. 进入 **Settings → Pages**。
8. Source 选择 **Deploy from a branch**。
9. Branch 选择 `main`，Folder 选择 `/ (root)`，点击 **Save**。
10. 等待 Pages 构建完成后访问 `https://yunyuzhu2005.github.io/ad-compliance-checker/`。

## 重要提醒

- 请上传解压后的文件，不要只上传 ZIP。
- `index.html` 必须位于仓库根目录，不能额外套一层目录。
- `static` 目录必须完整保留。
- 本工具仅用于发布前初审，不替代法务或合规终审。
